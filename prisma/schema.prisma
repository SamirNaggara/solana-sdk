datasource db {
  provider = "postgresql"
  url      = "postgresql://safeout:pide@localhost:4242/sdk-1?schema=public"
}

generator client {
  provider = "prisma-client-js"
}

model ProductDPP {
  id        String   @id
  info      Json
  dateReference Json
  signature String?
}
